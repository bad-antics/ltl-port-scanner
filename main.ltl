// ltl-port-scanner/main.ltl
//
// Connect-scan TCP port sweeper with banner grabbing.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-port-scanner v0.1.0")
    io::println("Connect-scan TCP port sweeper with banner grabbing.")
    0
}
